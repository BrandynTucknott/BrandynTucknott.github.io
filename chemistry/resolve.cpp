#ifndef HEADER_H
#define HEADER_H
#include "header.h"
#endif

void resolveChoice(std::string* current_element, int choice, std::string** element_symbols, const int NUM_ELEMENTS)
{
    // not currently working with a value
    if (*current_value == "")
        *current_value = getUserElements(element_symbols, NUM_ELEMENTS);

    switch (choice)
    {
        case 1:
            // get user input (mass: mg, g, kg)
    }
}